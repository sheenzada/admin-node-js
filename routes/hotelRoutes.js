// routes/hotelRoutes.js
const express = require('express');
const router = express.Router();

// Temporary placeholder routes
router.get('/', (req, res) => {
  res.json({ message: 'Hotel routes are not yet implemented' });
});

module.exports = router;